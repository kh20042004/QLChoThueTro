<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lịch Hẹn Xem Phòng - HomeRent</title>
    
    <!-- Tailwind CSS via jsDelivr -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/main.css">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .animate-slideDown {
            animation: slideDown 0.3s ease-out;
        }

        html { scroll-behavior: smooth; }
        
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: #F3F4F6;
        }
        
        ::-webkit-scrollbar-thumb {
            background: #9CA3AF;
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: #6B7280;
        }

        .property-info {
            display: flex;
            gap: 1rem;
        }

        .property-img {
            width: 120px;
            height: 90px;
            border-radius: 8px;
            object-fit: cover;
        }

        .status-badge {
            padding: 0.4rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .status-pending {
            background: #fff3cd;
            color: #997404;
        }

        .status-confirmed {
            background: #d1ecf1;
            color: #055160;
        }

        .status-completed {
            background: #d1e7dd;
            color: #0f5132;
        }

        .status-cancelled {
            background: #f8d7da;
            color: #842029;
        }

        .booking-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .detail-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: #6c757d;
        }

        .detail-item i {
            width: 20px;
            color: #667eea;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .btn-action {
            padding: 0.5rem 1.5rem;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s;
        }

        .filter-section {
            background: white;
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .empty-state i {
            font-size: 4rem;
            color: #dee2e6;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .property-info {
                flex-direction: column;
            }
            
            .booking-header {
                flex-direction: column;
                gap: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header sticky-top">
        <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
            <div class="container">
                <!-- Logo -->
                <a class="navbar-brand d-flex align-items-center" href="/">
                    <i class="fas fa-home text-primary fs-3 me-2"></i>
                    <span class="fw-bold text-primary">HomeRent</span>
                </a>

                <!-- Toggle button -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Menu -->
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto align-items-center">
                        <li class="nav-item">
                            <a class="nav-link" href="/">Trang chủ</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/properties">Tìm phòng</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/my-properties">Quản lý tin</a>
                        </li>
                        
                        <!-- User Dropdown -->
                        <li class="nav-item dropdown ms-lg-3">
                            <button class="nav-link dropdown-toggle d-flex align-items-center user-dropdown-btn" type="button" id="userDropdown" data-bs-toggle="dropdown">
                                <img src="https://via.placeholder.com/40/6B7280/ffffff?text=U" alt="Avatar" class="rounded-circle me-2" width="40" height="40" id="userAvatar">
                                <span class="text-dark fw-semibold" id="userName">Người dùng</span>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end user-dropdown-menu shadow-lg">
                                <li><a class="dropdown-item py-2" href="/profile">
                                    <i class="fas fa-user me-2 text-muted"></i>Hồ sơ cá nhân
                                </a></li>
                                <li><a class="dropdown-item py-2" href="/bookings">
                                    <i class="fas fa-calendar-alt me-2 text-muted"></i>Đơn đặt phòng
                                </a></li>
                                <li><a class="dropdown-item py-2 active" href="/landlord/bookings">
                                    <i class="fas fa-calendar-check me-2 text-muted"></i>Lịch hẹn xem phòng
                                </a></li>
                                <li><a class="dropdown-item py-2" href="/my-properties">
                                    <i class="fas fa-home me-2 text-muted"></i>Nhà/phòng của tôi
                                </a></li>
                                <li><a class="dropdown-item py-2" href="/settings">
                                    <i class="fas fa-cog me-2 text-muted"></i>Cài đặt
                                </a></li>
                                <li><hr class="dropdown-divider my-1"></li>
                                <li><a class="dropdown-item py-2 text-danger" href="/logout" onclick="handleLogout(event)">
                                    <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                                </a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- Page Header -->
    <div class="page-header">
        <div class="container">
            <h1 class="mb-2"><i class="fas fa-calendar-check me-2"></i>Lịch Hẹn Xem Phòng</h1>
            <p class="mb-0">Quản lý các lịch hẹn xem phòng của bạn</p>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container mb-5">
        <!-- Stats Cards -->
        <div class="row mb-4">
            <div class="col-md-3 col-6">
                <div class="stats-card pending">
                    <div class="icon">
                        <i class="fas fa-clock"></i>
                    </div>
                    <h3 class="mb-0" id="pendingCount">0</h3>
                    <p class="text-muted mb-0">Chờ xác nhận</p>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="stats-card confirmed">
                    <div class="icon">
                        <i class="fas fa-check-circle"></i>
                    </div>
                    <h3 class="mb-0" id="confirmedCount">0</h3>
                    <p class="text-muted mb-0">Đã xác nhận</p>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="stats-card completed">
                    <div class="icon">
                        <i class="fas fa-check-double"></i>
                    </div>
                    <h3 class="mb-0" id="completedCount">0</h3>
                    <p class="text-muted mb-0">Hoàn thành</p>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="stats-card cancelled">
                    <div class="icon">
                        <i class="fas fa-times-circle"></i>
                    </div>
                    <h3 class="mb-0" id="cancelledCount">0</h3>
                    <p class="text-muted mb-0">Đã hủy</p>
                </div>
            </div>
        </div>

        <!-- Filter Section -->
        <div class="filter-section">
            <div class="row g-3">
                <div class="col-md-3">
                    <label class="form-label fw-semibold">Trạng thái</label>
                    <select class="form-select" id="statusFilter">
                        <option value="">Tất cả</option>
                        <option value="pending">Chờ xác nhận</option>
                        <option value="confirmed">Đã xác nhận</option>
                        <option value="completed">Hoàn thành</option>
                        <option value="cancelled">Đã hủy</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-semibold">Sắp xếp</label>
                    <select class="form-select" id="sortFilter">
                        <option value="newest">Mới nhất</option>
                        <option value="oldest">Cũ nhất</option>
                        <option value="date-asc">Ngày hẹn tăng dần</option>
                        <option value="date-desc">Ngày hẹn giảm dần</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label class="form-label fw-semibold">Tìm kiếm</label>
                    <input type="text" class="form-control" id="searchInput" placeholder="Tìm theo tên, SĐT, địa chỉ...">
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <button class="btn btn-primary w-100" onclick="applyFilters()">
                        <i class="fas fa-filter me-1"></i>Lọc
                    </button>
                </div>
            </div>
        </div>

        <!-- Bookings List -->
        <div id="bookingsList">
            <!-- Bookings will be loaded here -->
        </div>

        <!-- Empty State -->
        <div id="emptyState" class="empty-state" style="display: none;">
            <i class="fas fa-calendar-times"></i>
            <h4 class="text-muted">Chưa có lịch hẹn nào</h4>
            <p class="text-muted">Khi có người đặt lịch xem phòng, thông tin sẽ hiển thị ở đây</p>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JS -->
    <script src="/js/landlord-bookings.js"></script>
</body>
</html>
